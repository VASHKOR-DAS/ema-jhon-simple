/**
 export app (the firebase config)
 */

/*
1. Create UserContext (Auth Context): UserContext --> component name
userContext provides AuthContext

2. Create AuthContext
3. set AuthContext.Provider
4. Make sure you set the children
5. export
6. use const authInfo = {} for AuthContext.Provider value={authInfo}
7. Go to index.js set <App/> for childer of UserContext
8. Get form data 
*/